<h1>DASHBOARD PROJET</h>
<br>
<p> <%= @project.name %> </p>

<% if @project.status == 'pending'%>
  <%= render 'pending' %>

<% elsif @project.status == 'ongoing' %>
  <%= render 'on_going' %>

<% else %>
  <%= render 'finished' %>

<% end %>



