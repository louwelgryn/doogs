<div class="img">
  <%= cl_image_tag(project.image, width: 200, quality: 30) %>
</div>

<div class="info-content">
  <h6><%= project.name %></h6>
  <h6 class="asso"><%= project.charity.name %></h6>
  <p><%= project.charity.address %></p>
</div>

<div class="btn-info-window">
  <%= link_to 'En savoir +', project_path(project), class: "blog-slider__button" %>
</div>
