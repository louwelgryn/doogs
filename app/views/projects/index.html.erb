this is the projects page
<div class="container">
  <% @projects.each do |project| %>
    <%= project.name %>
    <%= project.charity %>
    <%= project.description %>
    <%= project.development_goals %>
    <%= project.status %>
    <%= project.image %>
  <% end %>
</div>

