<div class="navbar navbar-expand-sm navbar-light navbar-lewagon <% unless current_page?(root_path) %>navbar-lewagon-white<% end %>">

  <%= link_to root_path, class: "navbar-brand" do %>
    <%= image_tag "DOOG.1.png" %>
  <% end %>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <%= link_to 'Les projets', projects_path, class: "btn-black", id: "btn-navbar" %>
    <% if user_signed_in? %>
      <%= cl_image_tag current_user.image, width: 120, height: 120, gravity: :face, crop: :thumb, class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
      <% if current_user.notifications > 0 %>
        <div id="navbar-notif-badge">
          <div id="badge-wraper">
            <%= current_user.notifications %>
          </div>
        </div>
      <% end %>

      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
        <span><i class="fas fa-user"></i> <%= current_user.name %></span>
        <%= link_to dashboard_path, class: "nav-link" do %>
          <div id="navbar-flexbox">
            <span>
              Mon tableau de bord
            </span>
          </div>
        <% end %>
        <%= link_to "Se déconnecter", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
      </div>

    <% else %>
        <%= link_to "Se connecter", new_user_session_path, class: "navbar-signin" %>
    <% end %>
  </div>
</div>


