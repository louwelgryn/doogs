<div class="navbar navbar-expand-sm navbar-light navbar-lewagon
  <% unless current_page?(root_path) %>
    navbar-lewagon-white
  <% end %>
 ">

  <%= link_to root_path, class: "navbar-brand cool-link" do %>
    <%= image_tag "GroupDoog.svg" %>
  <% end %>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <%= link_to projects_path, class: "btn-black" do %>
      <p>Les projets</p>
    <% end %>
    <% if user_signed_in? %>
      <%= cl_image_tag current_user.image, :transformation=>[{:gravity=>"face", :crop=>"crop"}], class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
        <span><%= current_user.name %></span>
        <%= link_to dashboard_path, class: "nav-link cool-link" do %>
          <div id="navbar-flexbox">
            <span>
              Mon tableau de bord
            </span>
            <% if current_user.notifications > 0 %>
              <span id="navbar-notif-badge">
                <div id="badge-wraper">
                  <%= current_user.notifications %>
                </div>
              </span>
            <% end %>
          </div>
        <% end %>
        <%= link_to "Se déconnecter", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
      </div>

    <% else %>
        <%= link_to "Se connecter", new_user_session_path, class: "nav-link cool-link" %>
    <% end %>
  </div>
</div>


