<% if @message.errors.any? %>

<%else%>

  var messagesContainer = document.querySelector('.messages');
  messagesContainer.insertAdjacentHTML('beforeend', " <%= j render 'messages/message', message: @message, user_is_messages_author: true  %>");
  // window.voteFunction();

  voteFunction();
  upVoteFunction();
  downVoteFunction();

  var input = document.querySelector('#message_content');
  input.value = '';
  input.blur();
  window.scrollLastMessageIntoView()
<%end%>

